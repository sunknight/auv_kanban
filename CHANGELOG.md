# Changelog

本项目所有面向用户的版本变动记录。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [0.4.2] - 2026-08-25
### Added
- 任务详情新增「打开目录」按钮，可直接用系统文件管理器（macOS Finder / Windows 资源管理器）打开该任务的实体目录，往里拖素材、看文档更方便。
- 任务目录里出现无法预览的文件（html/json/sql 等）或子目录时，打开详情会自动维护一份目录清单（`files.md`）：tree 风格递归列出全部文件（含子目录内），清单标签固定在「任务文档」标题行最右侧；智能体可在清单里给文件补一句话说明，清单结构更新时按相对路径保留（不同子目录里的同名文件不会串）。

### Changed
- 任务文档的标签按钮去掉文件图标、只显示文件名，固定文档更容易在同一行放下。

### Fixed
- 任务目录里若有指向看板外的软链，此前文件监视会跟随软链一路监视到任务目录之外，遇到特殊文件（如系统临时目录里的 socket）会直接打挂看板服务；现在监视不再跟随软链，服务不再因此崩溃。

## [0.4.1] - 2026-08-24
### Added
- 任务新增「延后事项」记录（todo.md）：专门记录本次不做、留待后续处理的事项（技术债、边界外需求、后续优化），与必须完成的子任务区分开，不阻塞任务完成、不计入进度。
- 有未完成的延后事项时，任务卡片显示 `todo` 徽标，已完成的任务同样显示以提醒遗留；全部处理完后徽标消失（文件保留可追溯）。
- 新增 `kanban todo-check` / `kanban todo-uncheck` 命令标记延后事项完成状态；`kanban show` 附延后事项清单，`kanban list` 对有遗留的任务标注。
- 延后事项支持多行内容：标题行下方可跟说明文字；内容里形似事项开头的行会自动转义避免误识别。
- Web 详情中 todo.md 以可点选清单展示，点勾选框即可标记完成；点「＋添加」填写标题与内容即可追加新事项（只追加、不动已有内容）；任务还没有 todo.md 时文档区提供「＋ todo.md」入口，提交后才创建文件。

## [0.4.0] - 2026-08-20
### Added
- 裸输 `/kanban` 或 `/kanban ?` 现在进入分步询问向导：先点选「执行任务 / 新建任务 / 补充需求 / 查看与速查」，选项按看板真实任务动态生成，问齐参数后直接开跑，不再需要记命令格式。
- 命令后加 `?` 可跳过第一问、直入对应分支（如 `/kanban run ?`、`/kanban create ?`、`/kanban update ?`）。
- 向导新增「常用命令速查」：输出一份顶部带看板现状、复制即用的命令速查表。

### Changed
- `/kanban update` 补充需求后默认立即执行刚补的那条，不必再手动执行一次；只想先补需求、稍后再做时加 `--no-run`。
- README 与 Web 帮助按最新功能更新：新增 Skill 与斜杠命令介绍、界面截图。

## [0.3.2] - 2026-07-31
### Added
- 新增 `/kanban create` 指令：一条消息给出标题和多行描述，即可创建任务并自动填好描述，随后直接进入执行——省去先建任务、再手填描述、再单独执行的步骤。
- `/kanban create --backfill` 可把会话进行中已经开干、但忘了从看板任务起步的工作补录为一个任务，任务挂在「进行中」继续推进，并在补录后询问下一步。

## [0.3.1] - 2026-07-31
### Added
- 任务详情改为左右布局：左侧编辑任务、右侧查看文档，弹窗整体加宽，文档预览区更舒展。
- 任务卡片新增「复制」按钮，可直接复制执行命令，无需进入详情；点击卡片任意位置即可打开任务详情。
- 编辑任务描述时内容会自动暂存，关闭弹窗、刷新页面或切换项目后可恢复；任务被外部（命令行、拖拽、智能体）改动后会自动同步磁盘最新内容。
- 任务详情新增「保存并继续」按钮，保存后不关闭弹窗，可继续编辑。

### Changed
- 任务详情的保存按钮移至左侧编辑区下方，作为表单的一部分。

## [0.3.0] - 2026-07-29
### Added
- `/kanban run` 新增 `--design` 参数（自然语言「预研/做设计/做方案」亦可），可对任务只做设计、产出设计与计划文档后停下待实施；下次再执行同一任务时会读取已有设计直接进入实施，不重复设计。
- 执行任务时，设计文档（`design.md`、`plan.md`）改为在设计阶段即生成保存，而非等到任务收尾才写。

### Changed
- 任务详情里的「描述」输入框现在随输入内容自动调高，到一定高度后停止增长并改为滚动，不再需要手动拖拽调整。

## [0.2.6] - 2026-07-28
### Added
- `kanban skill install` 现在可以把 Skill 装到 ZCode 以外的智能体（如 Claude、Codex、Gemini），不再只装 ZCode。
- 新增 `--agent` 指定要装的智能体（如 `--agent claude,codex`）、`--list` 查看本机已检测到的智能体、`--all` 给全部已知智能体安装。

### Changed
- `kanban skill install` 默认改为自动检测本机已安装的智能体并全部装上（此前固定只装 ZCode）。

## [0.2.5] - 2026-07-27
### Added
- Web 界面右上角新增「帮助」按钮，点击可查看快速入门与完整使用说明。
- 新增 `kanban uncheck` 命令，用于明确取消子任务勾选。

### Changed
- `kanban check` 不再翻转勾选状态，改为明确勾选（已是勾选态再执行不会变化），避免误操作。
