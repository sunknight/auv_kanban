# 项目约定

## 修改 kanban skill

修改 kanban skill 内容时，**只改项目内的普通文件**：

```
skill/kanban/SKILL.md
```

该文件会被软链到用户级 skill 目录（由用户环境建立软链，本仓库不假设其位置）。

**不要反向直接编辑用户级 skill 目录下的那份**（即软链的目标端）——原因：

- 用户级那份不保证软链回本项目，可能指向别处（另一个项目、或用户全局副本）。
- 直接改用户级那份 = 改错了对象，本仓库的 git 追踪不到，且可能影响其他用到该 skill 的项目。

正确做法：编辑 `skill/kanban/SKILL.md`（项目内相对路径），git 自然追踪这份；软链由用户环境负责同步生效。
